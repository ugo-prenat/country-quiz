import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  
    safeArea: {
        flex: 1,
    },

    container : {
        flex : 1,
        alignItems : 'center',
    },

    background_image: {
        flex: 1,
        justifyContent: 'center'
      },

    coutryButton : {
        borderColor : '#fff',
        borderWidth : 2,
        borderRadius : 5,
        width : 200,
        height : 50,
        marginBottom : 20,
    }
    
});
